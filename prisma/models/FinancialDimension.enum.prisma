enum FinancialDimension {
  DESENROLA
  ORGANIZA
  RESERVA
  INVESTE
}

enum GoalType {
  SHORT_TERM
  MEDIUM_TERM
  LONG_TERM
}

enum RiskLevel {
  LOW
  MEDIUM
  HIGH
}

// enum GoalCategory {
//   DEBT // metas de quitar dívidas (impacta Desenrola)
//   RESERVE // reserva de emergência
//   INVESTMENT // investimentos em geral
//   BUDGET // metas ligadas a organização de orçamento
//   OTHER
// }

enum CashFlowOperation {
  INCOME // dinheiro que entra
  EXPENSE // dinheiro que sai
  TRANSFER // movimentação interna (ex: enviar p/ meta)
}
